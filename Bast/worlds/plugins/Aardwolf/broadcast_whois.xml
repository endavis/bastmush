<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>
<!-- $Id: broadcast_level.xml 289 2009-03-16 16:23:51Z endavis $ -->
<muclient>
<plugin
   name="xBroadcast_Whois"
   author="Bast"
   id="aaaf9a8dc92f8fc614663ac0"
   language="Lua"
   purpose="broadcast info from whois"
   save_state="y"      
   date_written="2009-02-22 14:20:07"
   requires="4.38"
   version="1.0"
   >

<description trim="y">
<![CDATA[
[FOR PLUGIN AUTHORS ONLY]

Broadcasts stats when leveling
Broadcasts
   1 - whois info, get var.whoisinfo and loadstring it
    whoisinfo = {}
      whoisinfo.powerups = {}
        whoisinfo.powerups.mort = "15"
        whoisinfo.powerups.all = "25"
      whoisinfo["times killed"] = "164"
      whoisinfo.tiers = "0"
      whoisinfo["quest points"] = "20833"
      whoisinfo["campaigns done"] = "716"
      whoisinfo.race = "shade"
      whoisinfo["trivia points"] = "13"
      whoisinfo.level = "200"
      whoisinfo["quests failed"] = "129"
      whoisinfo["hours online"] = "4354"
      whoisinfo["combat maze deaths"] = "0"
      whoisinfo["qp earned"] = "117794"
      whoisinfo["trained stats"] = "820"
      whoisinfo["birth date"] = "15 Jul 2003"
      whoisinfo["quests complete"] = "5417"
      whoisinfo["campaigns fld"] = "34"
      whoisinfo["gquests won"] = "24"
      whoisinfo["monsters killed"] = "27621"
      whoisinfo.classes = {}
        whoisinfo.classes[1] = "thief"
        whoisinfo.classes[2] = "psionicist"
        whoisinfo.classes[3] = "cleric"
        whoisinfo.classes[4] = "paladin"
      whoisinfo["combat maze kills"] = "0"
      whoisinfo.name = "Bast"
      whoisinfo.sex = "male"
      whoisinfo["duels won"] = "0"
      whoisinfo["duels lost"] = "0"
      whoisinfo.subclass = "ninja"
      whoisinfo.remorts = 4 

Example
  function OnPluginBroadcast (msg, id, name, text)
    if id == "aaaf9a8dc92f8fc614663ac0" and msg == 1 then
      local pvar = GetPluginVariable(  "aaaf9a8dc92f8fc614663ac0", whoisinfo )

      -- get the info
      loadstring( pvar )()
      info = whoisinfo
    end
  end
]]>
</description>
</plugin>


<!--  Get our standard constants -->

<include name="constants.lua"/>

<triggers>
  <trigger
   script="whois_line1"
   match="^\[(\d+)\s+T(\d)\s+(\w+)\s+(\w+)\]\s+(\w+)\s+(.*)\s+\((\w+)\s+(\w+)\)$"
   enabled="n"
   regexp="y"
   ignore_case="y"
   sequence="100"
   group="whois"
  >
  </trigger> 
  <trigger
   script="whois_line2"
   match="^\[Multiclass Player: (.*) \]$"
   enabled="n"
   regexp="y"
   ignore_case="y"
   sequence="100"
   group="whois"
  >
  </trigger>   
  <trigger
   script="whois_stats"
   match="^(?&lt;name1&gt;[\w\s]*)\s*:\s*\[\s*(?&lt;val1&gt;[\w\d\s]*)\s*\]\s*(?&lt;name2&gt;[\w\s]*)\s*:\s*\[\s*(?&lt;val2&gt;[\w\d\s]*)\s*\]\s*$"
   enabled="n"
   regexp="y"
   ignore_case="y"
   sequence="101"
   group="whois"
  >
  </trigger>  
  <trigger
   script="whois_stats"
   match="^(?&lt;name1&gt;[\w\s]*)\s*:\s*\[\s*(?&lt;val1&gt;[\w\d\s]*)\s*\]\s*(?&lt;name2&gt;[\w\s]*)\s*:\s*\[\s*(?&lt;val2&gt;[\w\d\s]*)\s*\]\s*(?&lt;name3&gt;[\w\s]*)\s*:\s*\[\s*(?&lt;val3&gt;[\w\d\s]*)\s*\]\s*$"
   enabled="n"
   regexp="y"
   ignore_case="y"
   sequence="101"
   group="whois"
  >
  </trigger>  
  <trigger
   script="whois_stats"
   match="^(?&lt;name1&gt;[\w\s]*)\s*:\s*\[\s*(?&lt;val1&gt;[\w\d\s]*)\s*\]\s*$"
   enabled="n"
   regexp="y"
   ignore_case="y"
   sequence="101"
   group="whois"
  >
  </trigger>  
  <trigger
   name="powerup"
   script="whois_stats"
   match="^(?&lt;name1&gt;[\w\s]*)\s*:\s*\[\s*(?&lt;val1&gt;[\w\d\s]*)\s*\]\s*([\w\s]*)\s*:\s*\[\s*(?&lt;pval1&gt;[\w\d\s]*)\s*\]\s*\[\s*(?&lt;pval2&gt;[\w\d\s]*)\s*\]\s*$"
   enabled="y"
   regexp="y"
   ignore_case="y"
   sequence="101"
   group="whois"
  >
  </trigger>   
  <trigger
   name="whois_end"
   script="whois_end"
   match="^--------------------------------------------------------------------------$"
   enabled="n"
   regexp="y"
   ignore_case="y"
   sequence="101"
  >
  </trigger>  
</triggers>

<aliases>
  <alias
   name="whois_start"
   script="whois_start"
   match="^(whoi|whois)$"
   enabled="y"
   regexp="y"
   ignore_case="y"
   sequence="110"
   expand_variables="y"
  >
  </alias>  
</aliases>

<script>
<![CDATA[
require "var"
require "serialize"
require "commas"
require "pluginhelper"
require "verify"

-- keys - type, time, newlevel, hp, mana, mov, trains, pracs, bonustrains, bonusstats = {}
-- bonusstats subtable keys are the bonus stat names

currentlevel = ""
remorts = ""
level = ""
tier = ""
whoisinfo = {}

function whois_start(name, line, wildcards)
  whoisinfo = {}
  enabletriggroup("whois", true)
  Send(line)
end

function whois_line1(name, line, wildcards)
  whoisinfo['level'] = wildcards[1]
  whoisinfo['tiers'] = wildcards[2]
  whoisinfo['race'] = string.lower(wildcards[3])
  whoisinfo['name'] = wildcards[5]
  whoisinfo['sex'] = string.lower(wildcards[7])
  whoisinfo['subclass'] = string.lower(wildcards[8])
  whoisinfo['powerupsall'] = 0
  whoisinfo['powerupsmort'] = 0
  check(EnableTrigger("whois_end", true))    
end

function whois_line2(name, line, wildcards)
  tlist = utils.split(wildcards[1],"/")
  remorts = #tlist
  whoisinfo['remorts'] = remorts
  whoisinfo['classes'] = {}
  for _,tclass in ipairs(tlist) do
    tclass = string.gsub (tclass, " ", "")
    table.insert(whoisinfo['classes'], string.lower(trim(tclass)))
  end
end

function whois_stats(name, line, wildcards)
  for i = 1, 3 do
    local tname = 'name' .. i
    local tval = 'val' .. i
    if wildcards[tname] then
      tclass = string.gsub (wildcards[tname], " ", "")
      whoisinfo[string.lower(tclass)] = trim(wildcards[tval])
    end
  end
  if name == "powerup" then
  print("powerup")
    whoisinfo['powerupsall'] = trim(wildcards['pval1'])
    whoisinfo['powerupsmort'] = trim(wildcards['pval2'])
  end
end

function whois_end(name, line, wildcards)
  check(EnableTrigger("whois_end", false))  
  enabletriggroup("whois", false)
  whoisinfo['totallevels'] = (whoisinfo.remorts - 1) * 201 + whoisinfo.level
  whoisinfo['totaltrivia'] = 0
  var.whoisinfo = serialize.save( "whoisinfo", whoisinfo )  
  broadcast(1, var.whoisinfo)
end

function OnPluginEnable ()

end

function OnPluginDisable ()
  BroadcastPlugin(-1)
end


function OnPluginSaveState ()
  SetVariable ("enabled", tostring (GetPluginInfo (GetPluginID (), 17)))
  plugin_save_vars(options_table)    
end -- function OnPluginSaveState

function OnPluginInstall ()

 if GetVariable ("enabled") == "false" then
    ColourNote ("yellow", "", "Warning: Plugin " .. GetPluginName ().. " is currently disabled.")
    check (EnablePlugin(GetPluginID (), false))
    return
  end -- they didn't enable us last time
  
end -- OnPluginInstall
]]>
</script>

<aliases>
  <alias
   name="plugin_parse"
   script="plugin_parse"
   match="nothing"
   enabled="y"
   regexp="y"
   ignore_case="y"
   sequence="110"
   expand_variables="y"
  >
  </alias>   
</aliases>

<script>
<![CDATA[

options_table  = {
  tdebug = {help="set this for debugging information", type="bool", default=verify_bool(false)},
  plugin_colour = {help="set the plugin colour", type="colour", default=verify_colour("orange")},
  cmd = {help="the command for this plugin", type="string", after=set_plugin_alias, default="bwhois"},
}

cmds_table = {

}
  
function plugin_parse (name, line, wildcards)
  plugin_parse_helper(name, line, wildcards, cmds_table, options_table)
end -- plugin_parse

init_plugin_vars(options_table)
set_plugin_alias()
]]>
</script>
</muclient>
